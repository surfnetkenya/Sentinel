{
  "name": "sentinel-global",
  "version": "2.0.0",
  "type": "module",
  "description": "SENTINEL Global Intelligence Platform with Phase 2 Enhancements",
  "main": "api/index.js",
  "scripts": {
    "dev": "vercel dev",
    "build": "vercel build",
    "deploy": "vercel --prod"
  },
  "dependencies": {
    "node-fetch": "^3.3.2"
  },
  "engines": {
    "node": ">=18.x"
  }
}
